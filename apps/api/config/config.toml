[local.server]
port = 3001

[local.cors]
origins = ['http://localhost:5173']

# TODO(template): fill in real origins before deploying dev/prod.
# Only the active stage is validated, so these placeholders don't
# block local development.

[dev.server]
port = 3001

[dev.cors]
origins = []

[prod.server]
port = 3001

[prod.cors]
origins = []
